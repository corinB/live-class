# "test" 프로파일 — PostgreSQL Testcontainers 로 전환 (H2 base 설정을 오버라이드)
spring:
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: false
